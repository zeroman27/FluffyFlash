# Сессия 2026-04-15

## Контекст

Завершена и **проверена на железе** запись загрузочной USB: образ `28000.1_PROFESSIONAL_X64_EN-US.ISO`, флешка SanDisk, split `install.wim` на 2 части (~3800 MiB), успешное извлечение диска.

## Сделано (инженерно)

- **Убран rsync** для ISO→USB: вместо него **`WindowsISOFileCopy`** — обход смонтированного тома и `FileManager.copyItem` (аналог подхода WinDiskWriter без openrsync).
- Пропуски при копии: **`sources/install.wim`** / **`install.esd`** (далее `wimlib-imagex split`), **`boot.catalog`**, **`.DS_Store`**.
- Предпроверки: **`ISOFat32Precheck`**, **`find -size +3800m`**, оценка места **`VolumeBytes`** (`du` + `statfs`).
- **Журнал:** лимит строк увеличен; «Копировать лог» включает **`lastError`**.

## Документация

- Обновлены [[Architecture]], [[Roadmap]], [[Backlog]], [[Sessions/2026-04-14]] (дополнение).

## Дополнение: UX + E2E + метаданные USB

- **Нижняя панель** `WorkflowBottomBar` — главная кнопка «Сделать всё»; в detail — степпер «Источник → Кэш и ISO → Носитель».
- **`EndToEndMediaPipeline`:** UUP → `convert.sh` → ISO → запись на выбранные USB (параллель с лимитом; отдельный mount ISO на задачу).
- **`Wist.meta.json`** на томе после split (чтение старых **`WinForge.meta.json`** поддерживается); **`DiskManager`** подмешивает метаданные при refresh; карточка «Wist drives» + обновление до выбранной сборки.
- **USB:** мультивыбор в списке; точка монтирования через `diskutil info` (`diskXs1/s2`), не фиксированный путь.
- Документация vault обновлена: [[Architecture]], [[Roadmap]], [[Backlog]].

## Локализация (обновление vault)

- UI переведён на **английский** как базу в коде; пользовательские строки через `String(localized:)` / `String(format:)`.
- **`Localizable.xcstrings`** + локали **es, zh-Hans, hi, ar, fr, he**; RTL для ar/he — системно.
- Регенерация каталога: `python3 Scripts/build_localizable_xcstrings.py` из каталога проекта Xcode (`Wist/`).
- Подробности: раздел «Локализация» в [[Architecture]].

## Следующий шаг (опционально)

- Релиз на GitHub; углубление таблицы переводов в скрипте; отмена конвертации ISO; per-job лог в UI при параллельной записи.

## Ссылки

- [[Roadmap]] · [[Architecture]] · [[Backlog]]
